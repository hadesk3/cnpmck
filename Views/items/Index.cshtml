@model IEnumerable<WebApplication1.Models.item>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameItem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numberofInverntory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameItem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numberofInverntory)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idItem }) |
            @Html.ActionLink("Details", "Details", new { id=item.idItem }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idItem })
        </td>
    </tr>
}

</table>
