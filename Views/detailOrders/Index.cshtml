@model IEnumerable<WebApplication1.Models.detailOrder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.totalPriceItem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.item.nameItem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order.idAgency)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.totalPriceItem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.item.nameItem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order.idAgency)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
